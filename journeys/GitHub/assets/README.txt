Drop the requirements PDF here, named exactly:

    Contoso-Weather-Map-Requirements-v1.0.pdf

It is linked from WhyContoso.html (step 7), PlanTheWork.html (step 1) and index.html.
This folder is inside journeys/, which is on the publish whitelist, so it ships to the public site.
