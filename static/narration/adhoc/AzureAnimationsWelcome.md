# Narrator Script — Ad-hoc · Welcome to Azure Animations (~30s)

**Purpose:** A ~30-second spoken welcome + the "why" for the site, pointing to `aka.ms/AzureAnimations`.
**Not tied to a page** — this is a standalone intro clip (deliberately kept out of `static/v2/`).

Tags in `[brackets]` are delivery cues — speaking styles (e.g. `[confident]`) and
`[NNNms]` pause markers (e.g. `[600ms]`). One block = the whole 30-second read.

---

## Step 1 · Welcome

[encouraging] Welcome to Azure Animations — a friendlier way to understand the cloud.
[500ms]
[confident] Big ideas in Azure and A I often arrive as a wall of dense slides.
[500ms]
[optimistic] So we turn them into simple, step-by-step animations — one clear idea at a time.
[500ms]
[encouraging] No jargon overload. Just concepts that finally click.
[700ms]
[confident] Start exploring at aka dot m s slash Azure Animations.
