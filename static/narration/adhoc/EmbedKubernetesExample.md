# Narrator Script — Ad-hoc · Embed in PowerPoint → Kubernetes example (~24s)

**Purpose:** Transition clip after the Foundry walkthrough — makes the point that because the
animations are plain HTML they embed live inside a PowerPoint slide, then hands off to a second,
more complex example (the Kubernetes slide).
**Timeline slot:** 2:42–3:06 (~24s). Standalone clip (kept out of `static/v2/`).
**Audio:** `audio/EmbedKubernetesExample.mp3`.

Tags in `[brackets]` are delivery cues — speaking styles (e.g. `[impressed]`) and `[NNNms]` pauses.

---

## Step 1 · Embed live in PowerPoint → Kubernetes

[confident] And because Azure Animations is built on plain H T M L,
[300ms]
[impressed] it drops straight into a PowerPoint slide as a live, interactive view your audience can click and explore — no video files, no switching apps.
[500ms]
[curious] For another example, let's look at something far more complex — the Kubernetes slide.
