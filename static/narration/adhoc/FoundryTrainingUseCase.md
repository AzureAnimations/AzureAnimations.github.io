# Narrator Script — Ad-hoc · Microsoft Foundry training use-case (~29s)

**Purpose:** A ~29-second use-case pitch for a PPTX / demo — how Azure Animations makes AI training
more engaging, using the static "Microsoft Foundry overview" slide from course **AI-3016** as the
before-picture (dense bullet points → an animated explainer).
**Not tied to a page** — standalone pitch clip (deliberately kept out of `static/v2/`).

Tags in `[brackets]` are delivery cues — speaking styles (e.g. `[optimistic]`) and
`[NNNms]` pause markers (e.g. `[500ms]`). One block = the whole ~29-second read.

---

## Step 1 · The Use-Case

[reflective] Here's the Microsoft Foundry overview slide from course A I three-oh-one-six.
[500ms]
[serious] One dense slide — models, agents, tools, and knowledge — all crammed into bullet points.
[500ms]
[concerned] Learners are left to imagine how it all fits together.
[500ms]
[optimistic] What if it came alive, each piece building step by step?
[400ms]
[confident] That's how Azure Animations makes training truly engaging.
